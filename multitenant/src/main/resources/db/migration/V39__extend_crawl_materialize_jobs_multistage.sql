ALTER TABLE crawl_materialize_jobs
    ADD COLUMN IF NOT EXISTS stage VARCHAR(64),
    ADD COLUMN IF NOT EXISTS stage_message TEXT,
    ADD COLUMN IF NOT EXISTS stage_updated_at TIMESTAMPTZ,
    ADD COLUMN IF NOT EXISTS total_urls INTEGER NOT NULL DEFAULT 0,
    ADD COLUMN IF NOT EXISTS processed_urls INTEGER NOT NULL DEFAULT 0,
    ADD COLUMN IF NOT EXISTS tenant_id UUID NULL REFERENCES tenants(id) ON DELETE SET NULL,
    ADD COLUMN IF NOT EXISTS visibility VARCHAR(32) NOT NULL DEFAULT 'TENANT_BOUND',
    ADD COLUMN IF NOT EXISTS build_artifact BOOLEAN NOT NULL DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS bind_tenant BOOLEAN NOT NULL DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS import_general BOOLEAN NOT NULL DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS artifact_id UUID NULL REFERENCES product_dataset_artifacts(id) ON DELETE SET NULL,
    ADD COLUMN IF NOT EXISTS artifact_path TEXT,
    ADD COLUMN IF NOT EXISTS active_kb_version_id UUID NULL REFERENCES tenant_kb_versions(id) ON DELETE SET NULL,
    ADD COLUMN IF NOT EXISTS general_source_id UUID NULL REFERENCES general_sources(id) ON DELETE SET NULL,
    ADD COLUMN IF NOT EXISTS import_run_id UUID NULL REFERENCES general_import_runs(id) ON DELETE SET NULL;
